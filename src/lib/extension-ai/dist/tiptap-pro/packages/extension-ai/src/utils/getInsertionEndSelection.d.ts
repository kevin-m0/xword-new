import { Selection, Transaction } from '@tiptap/pm/state';
export declare const getInsertionEndSelection: (tr: Transaction, startLen: number, bias: number) => -1 | Selection;
export default getInsertionEndSelection;
